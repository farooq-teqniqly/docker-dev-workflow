{
  "name": "nodeapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run-dev": "nodemon",
    "docker:build-dev": "docker image build -t sample-node-app-dev -f Dockerfile.dev .",
    "docker:run-dev": "docker container run --rm -it --name my-sample-node-app-dev --volume %cd%:/app -p 3000:3000 sample-node-app-dev",
    "docker:build": "docker image build -t sample-node-app .",
    "docker:run": "docker container run --rm -it --name my-sample-node-app --volume %cd%:/app -p 3000:3000 sample-node-app"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
